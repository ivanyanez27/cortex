/** Next.js configuration:
 *  - Enables React strict mode for highlighting potential issues during development
 *  - Use this file to customize webpack, images, redirects, etc. as needed
 *  See: https://nextjs.org/docs/app/api-reference/next-config-js
 */
/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  experimental: {
  }
};

export default nextConfig;


